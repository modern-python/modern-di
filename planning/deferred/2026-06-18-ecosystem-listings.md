---
summary: Get listed where newcomers look — `awesome-dependency-injection-in-python`, each host framework's third-party/ecosystem page, and rival comparison pages; zero-cost durable discovery, drafted but never executed.
---

# Get listed where newcomers look

Three discovery surfaces, all zero-cost and durable once done:

- `awesome-dependency-injection-in-python`
- each host framework's third-party / ecosystem page
- rival comparison pages — dishka, for instance, publishes an `alternatives.html`

## Why it is open

These are the places someone lands when they go looking for a Python DI container
without already knowing the names. Being absent from them means being invisible to
exactly the audience the adoption strategy targets, regardless of how good the
library is.

It was drafted as a section of the org launch playbook and never executed. There
is no technical work here — it is submissions and pull requests to other people's
lists.

## Revisit trigger

The launch window.
