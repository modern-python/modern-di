---
summary: Produced planning/audits/2026-07-22-blessed-ready-onramp-report.md scoring all 12 integrations — 1 blessed-ready (litestar), 11 not yet (fastapi closest, only D3=1) — with a §4 backlog ranking canonical-starter templates (10 of 12 lack one) as the top cross-cutting fix thread.
---

# Design: Blessed-ready on-ramp audit

## Summary

Produce one read-only audit report,
`planning/audits/2026-07-22-blessed-ready-onramp-report.md`, that scores each of
the 12 framework integrations against an explicit **blessed-ready** on-ramp
rubric and ranks the gaps into a fix backlog. The audit ships no code or doc
change to any repo; it is the ground-truth deliverable that the adoption
research's A2 item ("make the integrations blessed-ready") named but was **never
produced**, and it is the prerequisite that decides which downstream thread —
the `@inject` asymmetry, the missing FastStream/Typer templates, or a
README-normalization pass — is worth doing first.

## Motivation

A2 in the [2026-06-18 adoption research](../audits/2026-06-18-adoption-strategy-report.md)
is the stated growth bottleneck: adoption compounds through *being the
path-of-least-resistance DI inside a host framework*, and a framework only
blesses what a newcomer can adopt in one sitting. The report flags A2 as "never
audited" — so any rollout today would fix integrations blind.

A quick read of the 13 sibling repos (all checked out locally) already shows the
audit will find real, uneven gaps rather than confirm uniformity:

- **No integration ships a runnable `examples/` dir.** The only canonical
  starters are the FastAPI and Litestar SQLAlchemy templates, linked from those
  two READMEs as `Usage example: <template>`. The other 10 link only a generic
  "browse templates" footer.
- **The `@inject` split is 4 vs 8, not the report's "7 of 12."** Decorator-free:
  fastapi, litestar, faststream, taskiq. Require `@inject`: flask, starlette,
  aiohttp, celery, arq, aiogram, typer, **grpc** (the one the report missed).
- Bringing the integrations onto 3.x surfaced root-open lifecycle traps
  (change [2026-07-21.01](2026-07-21.01-integration-open-lifecycle-lessons.md)),
  so "does `setup_di` own the lifecycle for the user" varies in practice.

The number-level disagreements alone justify a measured pass before any fix
work commits effort.

## Design

**Method: source + docs read-through.** No apps are run; the "sub-5-min
onboarding" claim is measured by a step-count proxy, not a stopwatch. Every
score cites a `file:line` (or README section) in a named repo so each finding is
independently refutable — the house rule for audit claims.

**Subjects (12).** aiogram, aiohttp, arq, celery, fastapi, faststream, flask,
grpc, litestar, starlette, taskiq, typer. `modern-di-pytest` is excluded (a test
plugin, different on-ramp shape) and gets a one-line appendix, not a score.

**Evidence sources, all local:**
1. sibling repo `README.md`;
2. this repo's `docs/integrations/<name>.md` guide;
3. the sibling's public API — `setup_di` + the marker / `inject` code;
4. canonical-example / template link presence.

**Rubric — the blessed-ready definition** (a deliverable in itself; A2 never
defined one). Each subject scored **0 / 1 / 2** per dimension against the bars
locked below; §1 of the report restates them verbatim, this spec is their source.
Bars are written so the score is determined by a countable fact, not a judgment
call:

1. **One-call setup** — count the distinct DI wiring actions the user performs to
   activate the integration (register the container + open per-request scope +
   bind lifecycle), where an action ∈ {call `setup_di`; add a middleware/hook;
   register a plugin/extension; open/close the root by hand}. **2:** one action
   (a single `setup_di(app, container)` does all of it). **1:** exactly two
   actions. **0:** three or more, or the user hand-assembles middleware.
2. **Canonical example present + linked** — **2:** README links a dedicated,
   runnable starter as `Usage example: <repo>` (today: FastAPI, Litestar).
   **1:** the README's inline Usage block is complete and copy-paste-runnable but
   there is no dedicated starter (generic "browse templates" footer only).
   **0:** no complete runnable example — only fragmentary snippets.
3. **Lifespan handled for the user** — **2:** `setup_di` owns *both* root
   open/close and per-request (child-scope) open/close; the user writes no
   lifecycle code and there is no documented deployment context where the root
   fails to open. **1:** `setup_di` owns one side but the user must own the other,
   *or* there is a documented deployment caveat where the root's open hook does
   not fire (the root-open traps: mounted ASGI sub-app / disabled lifespan,
   taskiq `run_startup=False`, FastStream `TestBroker`, Celery non-prefork pools).
   **0:** the user must wire both root and child lifecycle by hand.
4. **Steps-to-first-dependency** (sub-5-min proxy) — count **L**, the DI-specific
   lines in a *minimal single-dependency* quickstart: imports of
   modern-di/integration symbols, the `Group` + its one provider, the `Container`
   construction, `setup_di`, the per-handler marker/decorator, and any manual
   lifecycle hook — excluding the framework app object, the business class body,
   and the handler body. **2:** L ≤ 7. **1:** 8 ≤ L ≤ 9. **0:** L ≥ 10.
   (Calibration: a decorator-free integration lands ≈7; `@inject` adds one line;
   a manual lifecycle hook adds one or two more.)
5. **`@inject` presence + avoidability** — **2:** no DI decorator at the handler
   (fastapi, litestar, faststream, taskiq). **1:** `@inject` required *and*
   inherent — the framework exposes no parameter-injection seam, so no adapter
   could remove it (e.g. Flask, Typer). **0:** `@inject` required *but avoidable*
   — the framework offers a seam the adapter does not yet use. Deciding 1 vs 0 per
   integration is the substance this dimension contributes to the downstream
   `@inject`-asymmetry thread.
6. **README consistency** — against the shared structure: brand lockup + badge
   block, one-line intro + `Full guide` link + canonical-example line,
   `## Installation`, `## Usage` (with the lifecycle/ordering note where it
   applies), footer. **2:** all elements present and in order. **1:** exactly one
   element missing or out of order. **0:** two or more missing, or a divergent
   format.

**Verdict rule (locked).** A subject is **blessed-ready** iff it scores **2 on
dimensions 1, 2, and 3** (the newcomer-facing essentials — one-call setup,
canonical example, lifecycle handled) **and has no 0 in any dimension**.
A dimension-5 score of **1** (inherent `@inject`) does *not* block the verdict —
only dimension-5 = 0 (avoidable-but-not-avoided) does. Everything else is
**not yet**, and §4 ranks its shortfall.

The per-dimension score drives the verdict and the ranking; the §3 prose carries
the finding. The per-subject sum (0–12) is reported only as a coarse sort key,
not claimed as a precise measure.

**Report shape** (`planning/audits/2026-07-22-blessed-ready-onramp-report.md`):
- **§1 Rubric** — the blessed-ready definition + per-dimension bar.
- **§2 Scorecard** — 12 rows × 6 dimensions + verdict.
- **§3 Per-integration notes** — one short paragraph of specific gaps each.
- **§4 Prioritized backlog** — fixes ranked by leverage × cost, each tagged with
  the downstream thread it becomes (`@inject` asymmetry / templates /
  README-normalization / per-integration). This is what the next spec picks up.
- **§5 Cross-cutting findings** — e.g. no `examples/` dirs anywhere; the real
  `@inject` split and its pattern; only 2 canonical templates.

## Non-goals

- No code or doc edits to any repo — measurement + prioritization only; fixes
  are the change files this audit spawns.
- No template building, no `@inject` unification — those are the downstream
  threads §4 ranks, each its own spec.
- No hands-on app builds — read-through only; step-count is a proxy, stated as
  such.
- `modern-di-pytest` not scored (appendix mention only).
- No `architecture/` promotion — an audit reports on sibling repos and changes
  no capability of this one.

## Testing

The report is the artifact. `just check-planning` validates change bundles, not
audit reports, so verification is: (1) every score in §2 traces to a cited
`file:line` / README section; (2) §2, §3, and §5 agree on each integration
(no scorecard row contradicting its own note); (3) the `@inject` split and
template-link counts in §5 match a fresh `grep` of the sibling repos at audit
time. `just lint-ci` stays green (it validates the change bundle).

## Risk

- **Read-through misreads a runtime lifecycle as manual when `setup_di` handles
  it** (likelihood: medium, impact: medium) — mitigate by reading the actual
  `setup_di` body for dimensions 1 and 3, not the README prose alone.
- **Step-count proxy over/under-states real onboarding** (medium × low) — it is
  labelled a proxy; the hybrid spot-build was explicitly declined, so this is an
  accepted bound, not a defect.
- **Facts drift between audit and fix** (low × low) — §5 re-greps the counts at
  audit time so the report is self-dated; fix changes re-verify against their
  target repo anyway.
